

scriptPath="$(cd "${0%/*}" 2>/dev/null; echo "$PWD"/"${0##*/}")"
scriptDir=`dirname "$scriptPath"`


#echo "In script PWD:"`pwd`;
#echo "TOOL_HOME: $TOOL_HOME";
java -jar $scriptDir/../lib/CsvCruncher-*-single.jar "$@"


## Cleanup
rm -rf ./hsqldb
